[package]
name = "usernames-fixtures"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Builders for the ENS gateway requests and store bindings the usernames-api integration suites send."
publish = false

[dependencies]
alloy.workspace = true
usernames-core.workspace = true
